"""Bundled default prompts and pipelines shipped with bog-agents-cli.

Content lives under ``bog_agents_cli/defaults/`` inside the wheel and is
seeded to ``~/.bog-agents/`` on first run via :mod:`bog_agents_cli.defaults_seeder`.
"""
