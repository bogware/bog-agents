# Phase 18 — 2026-05-13 (engineering-craft library doubled to 30)

Eighteenth real-world pass. Pure curation work — no LLM cost, no
experiment. Phase 15 validated the ``engineering-craft`` seed
category at 15 entries; Phase 18 doubles it to 30 to reduce seed
repetition under daily-dreaming load.

## TL;DR

* **15 new engineering-craft seeds** appended to the library.
* **Test floor moved from 15 → 30** in
  ``TestDreamSeeds::test_engineering_craft_category_present_and_curated``.
* **No experiment**, no LLM calls, no behavior change beyond reducing
  same-seed-twice frequency.

## The new seeds

The Phase 15 seeds focused on long-lived-system surprises (the
6-year-old bug, the feature flag from 2017, the migration script
collation difference). Phase 18 broadens the texture without
changing it:

```
The shared dictionary mutated in five threads that worked for three years because nobody held it long.
The retry loop that succeeded on the seventh attempt and was treated as a permanent fix.
The race condition that only appeared when the engineer who knew about it was on vacation.
The cron job that drifted forty-five minutes over four years because nobody noticed the timezone.
The 'temporary' shell script in /opt that ran every five minutes for the entire life of the company.
The integration test that passed because both halves of it were broken in canceling ways.
The float comparison that worked for years until a sensor with three more digits of precision was added.
The error message no one read because the rate of false positives had quietly crossed 50 percent.
The connection pool that never grew because the metric measuring its size was itself in the pool.
The fixed-size buffer that was correct in 2012 and slowly went out of fashion alongside the unit it measured.
The merge that resolved a conflict by deleting both sides, ship-tested for six weeks, never noticed.
The system that scaled cleanly to a thousand users because it crashed at user one hundred and was rewritten.
The graceful shutdown that took six minutes to flush a queue nobody was reading.
The encoding bug that only appeared when a user with an accent in their name signed up.
The performance fix that was a regression in disguise because the slow path had been suppressing a bug.
```

Six thematic clusters:

1. **Thread-safety + shared-state failure modes** (dict-in-five-threads,
   race condition on vacation)
2. **Retry / cron / timing-dependent bugs** (retry-loop-permanent,
   cron drift, temporary script)
3. **Metric self-reference and observability blind spots**
   (connection pool measuring itself, error message rate crossing 50%)
4. **Drift over years** (encoding bug, float precision, fixed-size
   buffer, kilometers-in-feet shift)
5. **Merge resolutions that ship and never surface** (delete-both-sides,
   crash-at-user-100)
6. **Performance fixes that are regressions in disguise** (the slow
   path was suppressing a bug)

Each follows the Phase 15 shape: short observation, specific
detail, no historical figure, the texture is the *day-to-day craft*
of long-lived software.

## Why 30 entries

At production cadence (one dream every ~30 minutes during dormancy),
an agent would cycle through 15 seeds in roughly **7.5 hours**. Two
dreams within the same workday could pull the same seed, which
hurts variety. Doubling to 30 doubles the cycle time and roughly
halves the same-seed-twice probability per agent-day. At 30
entries, an engineer who dreams once an hour for a workday would
sample less than a third of the library — variety preserved.

## Cost

| Metric | Phase 18 |
|---|---|
| Real LLM calls | **0** |
| Wall clock | (curation only) |
| Approx cost | **$0** |

## Verdict

**READY TO MERGE.** Pure data; no experiment to validate beyond
"the library is bigger and the floor test passes." Phase 16 will
test whether the larger library changes outcomes when imagination
injection runs on technical prompts.

## Next steps

* Phase 16 (running): direct effectiveness vs no-injection control.
* Phase 17 (next): per-prompt routing for decision-shaped prompts.
